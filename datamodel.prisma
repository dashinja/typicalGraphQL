type User {
  id: ID! @id
  name: String!
  # createdAt: DateTime! @createdAt
  # updatedAt: DateTime! @updatedAt
  grade: [Grade]
}

type Grade {
  id: ID! @id
  grade: Float
  # createdAt: DateTime! @createdAt
  # updatedAt: DateTime! @updatedAt
  owner: User
}
